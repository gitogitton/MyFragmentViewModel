<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Toolbarのスタイル定義-->
    <style name="MainMenu" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@color/color_toolbar_backgroud</item>
        <item name="android:textColorPrimary">@color/color_toolbar_text</item>
        <item name="android:textColorSecondary">@color/color_toolbar_text</item>
    </style>

</resources>